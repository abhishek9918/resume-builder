import { Environment } from '../app/core/interface/environment.interface';

export const environment: Environment = {
  production: false,
  apiUrl: 'http://localhost:3132',
  deployedEnv: 'Local ✅',
};
