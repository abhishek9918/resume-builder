import { BlurOnScreenshotDirectiveDirective } from './blur-on-screenshot-directive.directive';

describe('BlurOnScreenshotDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurOnScreenshotDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
