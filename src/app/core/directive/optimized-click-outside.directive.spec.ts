import { OptimizedClickOutsideDirective } from './optimized-click-outside.directive';

describe('OptimizedClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new OptimizedClickOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
